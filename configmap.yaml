apiVersion: v1
kind: ConfigMap
metadata:
  name: telehealth-config
  namespace: default
data:
  config.path: "/etc/telehealth/config"
  app.environment: "development"
  log.level: "INFO"
  db.host: "localhost"
  db.port: "5432"
