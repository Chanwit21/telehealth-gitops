apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - argocd-namespace.yaml

namespace: default

commonLabels:
  app: telehealth-app
  managed-by: argocd
